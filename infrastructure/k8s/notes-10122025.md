# Notes
- Show the current namespace
```bash
k config get-contexts
```
- Create a new namespace
```bash
k create namespace test1
k create ns test2
```

- Switch to a different namespace
```bash
k config set-context --current --namespace test2
```

- Apply a manifest to a different namespace
```bash
k apply -f manifest.yaml --namespace test2
